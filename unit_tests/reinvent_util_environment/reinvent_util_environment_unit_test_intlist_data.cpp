#include <util/reinvent_util_errno.h>
#include <reinvent_util_environment_unit_test_intlist_data.h>

const IntListTest REINVENT_UTIL_ENVIRONMENT_INTLIST_DATA[] = {
    {
        __LINE__,
        0, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_0", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        1, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_1", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        2, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_2", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        3, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_3", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        4, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_4", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        5, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_5", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        6, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_6", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        7, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_7", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        8, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_8", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        9, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_9", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        10, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_10", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        11, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_11", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        12, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_12", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        13, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_13", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        14, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_14", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        15, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_15", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        16, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_16", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        17, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_17", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        18, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_18", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        19, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_19", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        20, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_20", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        21, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_21", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        22, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_22", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        23, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_23", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        24, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_24", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        25, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_25", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        26, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_26", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        27, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_27", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        28, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_28", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        29, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_29", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        30, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_30", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        31, // test number
        "Malformed integer-list not in UNIX env",
        "INTLIST_ENV_VAR_31", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_NOTFOUND, // expected return code
        false, // install a value into UNIX ENV pre-test?
        "-1", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        32, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_32", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "a", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        33, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_33", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "0x1213", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        34, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_34", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "ABC", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        35, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_35", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " 9", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        36, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_36", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9 ", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        37, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_37", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "392 33", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        38, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_38", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9, 10, 11", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        39, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_39", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " 9,10,11", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        40, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_40", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,10 , 11", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        41, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_41", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,10,11 ", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        42, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_42", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        ",9,10", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        43, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_43", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " ,9,10", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        44, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_44", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,10,9,10, ", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        45, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_45", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,9]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        46, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_46", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " [10,13]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        47, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_47", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,13] ", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        48, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_48", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10 9]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        49, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_49", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        50, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_50", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        51, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_51", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10, ]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        52, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_52", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,  ]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        53, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_53", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[,1]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        54, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_54", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[, 1]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        55, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_55", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[,1 ]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        56, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_56", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[1,],10,12", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        57, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_57", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[,1],10,12", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        58, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_58", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,[1,],12", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        59, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_59", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,[,1],12", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        60, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_60", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[1,]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        61, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_61", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[,1]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        62, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_62", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[1,],5", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        63, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_63", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[,1],5", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        64, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_64", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "a", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        65, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_65", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "0x1213", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        66, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_66", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "ABC", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        67, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_67", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " 9", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        68, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_68", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9 ", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        69, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_69", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "392 33", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        70, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_70", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9, 10, 11", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        71, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_71", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " 9,10,11", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        72, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_72", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,10 , 11", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        73, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_73", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,10,11 ", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        74, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_74", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        ",9,10", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        75, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_75", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " ,9,10", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        76, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_76", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,10,9,10, ", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        77, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_77", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,9]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        78, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_78", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " [10,13]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        79, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_79", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,13] ", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        80, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_80", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10 9]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        81, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_81", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        82, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_82", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        83, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_83", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10, ]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        84, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_84", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,  ]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        85, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_85", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[,1]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        86, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_86", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[, 1]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        87, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_87", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[,1 ]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        88, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_88", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[1,],10,12", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        89, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_89", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[,1],10,12", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        90, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_90", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,[1,],12", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        91, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_91", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,[,1],12", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        92, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_92", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[1,]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        93, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_93", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[,1]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        94, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_94", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[1,],5", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        95, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_95", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[,1],5", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // non-default-range min value
        -1, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        96, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_96", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "a", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        97, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_97", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "0x1213", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        98, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_98", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "ABC", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        99, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_99", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " 9", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        100, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_100", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9 ", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        101, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_101", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "392 33", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        102, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_102", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9, 10, 11", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        103, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_103", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " 9,10,11", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        104, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_104", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,10 , 11", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        105, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_105", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,10,11 ", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        106, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_106", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        ",9,10", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        107, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_107", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " ,9,10", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        108, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_108", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,10,9,10, ", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        109, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_109", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,9]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        110, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_110", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        " [10,13]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        111, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_111", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,13] ", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        112, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_112", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10 9]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        113, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_113", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        114, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_114", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        115, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_115", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10, ]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        116, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_116", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[10,  ]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        117, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_117", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[,1]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        118, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_118", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[, 1]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        119, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_119", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[,1 ]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        120, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_120", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[1,],10,12", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        121, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_121", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[,1],10,12", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        122, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_122", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,[1,],12", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        123, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_123", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,[,1],12", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        124, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_124", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[1,]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        125, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_125", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[,1]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        126, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_126", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[1,],5", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        127, // test number
        "Malformed integer-list in UNIX env",
        "INTLIST_ENV_VAR_127", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_ENVIRONMENT, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,12,[,1],5", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        128, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_128", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        129, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_129", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "0", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        130, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_130", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "1,2", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        131, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_131", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "3,4,5", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        132, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_132", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,11,12,13,100", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        133, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_133", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,1,3,2,100,43,16", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        134, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_134", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[0,5]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        135, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_135", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        136, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_136", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,102]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        137, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_137", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[9991,9999]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        138, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_138", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,[100,100],3,[100,103],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        139, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_139", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[100,103],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        140, // test number
        "Valid integer-list in UNIX env not range tested",
        "INTLIST_ENV_VAR_140", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[100,109],2,0,[422,424]", // value to install into UNIX ENV or -1 not used. can be malformed
        false, // is value subject to range verification?
        false, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        141, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_141", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        142, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_142", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        143, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_143", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "1,2", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        144, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_144", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "3,4,5", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        145, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_145", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,11,12,13,100", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        146, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_146", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,1,3,2,100,43,16", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        147, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_147", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[0,5]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        148, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_148", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        149, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_149", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,102]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        150, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_150", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[9991,9999]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        151, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_151", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,[100,100],3,[100,103],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        152, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_152", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[100,103],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        153, // test number
        "Valid integer-list in UNIX env inside custom range",
        "INTLIST_ENV_VAR_153", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[100,109],2,0,[422,424]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        -100, // non-default-range min value
        10000, // non-default-range man value
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        154, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_154", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        155, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_155", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        156, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_156", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "1,2", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        157, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_157", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "3,4,5", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        158, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_158", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,11,12,13,100", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        159, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_159", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,1,3,2,100,43,16", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        160, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_160", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[0,5]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        161, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_161", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        162, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_162", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,102]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        163, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_163", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[9991,9999]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        164, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_164", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,[100,100],3,[100,103],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        165, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_165", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[100,103],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        166, // test number
        "Valid integer-list in UNIX env outside custom range",
        "INTLIST_ENV_VAR_166", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[100,109],2,0,[422,424]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        false, // if range verification, is default range used?
        999999, // non-default-range min value
        1000000, // non-default-range man value
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        167, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_167", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        168, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_168", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        169, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_169", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "1,2", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        170, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_170", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "3,4,5", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        171, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_171", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,11,12,13,100", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        172, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_172", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,1,3,2,100,43,16", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        173, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_173", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[0,5]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        174, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_174", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        175, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_175", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,102]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        176, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_176", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[9991,9999]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        177, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_177", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9,[100,100],3,[100,103],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        178, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_178", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[100,103],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        179, // test number
        "Valid integer-list in UNIX env inside default range",
        "INTLIST_ENV_VAR_179", // variable to be tested
        0, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[100,109],2,0,[422,424]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        true, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        180, // test number
        "Valid integer-list in UNIX env outside default range",
        "INTLIST_ENV_VAR_180", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "9000000", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        181, // test number
        "Valid integer-list in UNIX env outside default range",
        "INTLIST_ENV_VAR_181", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "1000000,2", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        182, // test number
        "Valid integer-list in UNIX env outside default range",
        "INTLIST_ENV_VAR_182", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "3,4000000,5", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        183, // test number
        "Valid integer-list in UNIX env outside default range",
        "INTLIST_ENV_VAR_183", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,11,12,13000000,100", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        184, // test number
        "Valid integer-list in UNIX env outside default range",
        "INTLIST_ENV_VAR_184", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "10,1,3,2,100,43,16000000", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        185, // test number
        "Valid integer-list in UNIX env outside default range",
        "INTLIST_ENV_VAR_185", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[5000000,5000001]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        186, // test number
        "Valid integer-list in UNIX env outside default range",
        "INTLIST_ENV_VAR_186", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "90000000,[100,100],3,[100,103],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        187, // test number
        "Valid integer-list in UNIX env outside default range",
        "INTLIST_ENV_VAR_187", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[10000000,10300000],2,0", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
    {
        __LINE__,
        188, // test number
        "Valid integer-list in UNIX env outside default range",
        "INTLIST_ENV_VAR_188", // variable to be tested
        Reinvent::Util::Errno::REINVENT_UTIL_ERRNO_RANGE_LIST, // expected return code
        true, // install a value into UNIX ENV pre-test?
        "[100,100],3,[100,109],20000000,0,[422,424]", // value to install into UNIX ENV or -1 not used. can be malformed
        true, // is value subject to range verification?
        true, // if range verification, is default range used?
        -1, // range min unused or useDefaultRange true
        -1, // range max unused or useDefaultRange true
        false, // after test is done should there be a value in cache?
    },
};

int numReinventUtilEnvironmentIntListData() {
    return sizeof REINVENT_UTIL_ENVIRONMENT_INTLIST_DATA / sizeof *REINVENT_UTIL_ENVIRONMENT_INTLIST_DATA;
}

const IntListTest *reinventUtilEnvironmentIntListData(int i) {
    return (i>=0&&i<numReinventUtilEnvironmentIntListData()) ? &(REINVENT_UTIL_ENVIRONMENT_INTLIST_DATA[i]) : 0;
}

