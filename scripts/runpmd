#!/bin/bash

#
# run as sudo
#

echo Typical first time commands:
echo 1. show port info 0
echo
echo

TASK="${H}/dpdk/build/app/dpdk-testpmd"
ARGS="--proc-type primary --in-memory --log-level 7 -n 12 -l 4,5,6,7,8 --allow 01:00.1,class=eth -- -i --numa --eth-link-speed 25000"
$TASK $ARGS
